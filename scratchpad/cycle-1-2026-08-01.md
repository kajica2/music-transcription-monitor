# Swarm: Agentic Music Transcription Monitor — Cycle 1

**Date:** 2026-08-01 19:20 UTC+2 (Central European Summer Time)
**Orchestrator:** mavis (root session `mvs_228cda811d104ef6b8fa29c54ca9f42f`)
**Termination rule:** pass-all
**Wave size:** 5 parallel subagents
**Wall-clock budget:** 15 min (weekly routine ceiling)
**Actual wall-clock:** ~7 min for the swarm + 2 min verify + 1 min synthesis ≈ **10 min**

---

## Slices

| # | Slice | Worker type | Status | Artifact |
|---|-------|-------------|--------|----------|
| 1 | arXiv scan (cs.SD / eess.AS / cs.AI) | research (general) | **PASS** | `reports/weekly_2026-08-01_arxiv.md` (18.5 KB, 10 papers) |
| 2 | GitHub pulse (8 repos) | research (general) | **PASS** | `reports/weekly_2026-08-01_github.md` (8/8 attempted, 7/8 covered, 1 404) |
| 3 | Foundation: AGENTS.md + scaffolding + pipeline_starter.py | build (general) | **PASS** | `AGENTS.md` (118 lines), `setup/pipeline_starter.py` (valid Python), `trusted_packages.txt` (14 pkgs), `logs/agent_actions.log` (2 lines) |
| 4 | Dashboard: vanilla HTML+JS page | build (general) | **PASS** | `dashboard/index.html` (915 lines, 31 KB, 0 external resources) |
| 5 | Asset research (test audio + eval set) | research (general) | **PASS** | `reports/asset_research_2026-08-01.md` (6 test sources + 13 eval sources) |

All 5 STATUS.json files in `out/<slice>/` are PASS.

---

## Verification (read-back, not self-reports)

- arXiv report: 10 papers, each with arXiv ID, URL, abstract excerpt, 2-sentence summary, reproducibility note (✅ MuScriptor / VocalParse / Text2Score ship code; ⚠️ others paper-only). Hot Take names MuScriptor as the most impactful. Search Trail included. ✓
- GitHub report: Summary table covers all 8 repos. omnizart 404 documented with fallback suggestion. langgraph v1.2.5 + crewAI v1.15.3a2 release tags verified on Releases pages. basic-pitch flagged dormant (3+ years no release). Hot Take ranking present. ✓
- AGENTS.md: 118 lines, covers project goal, three routines (weekly/monthly/quarterly), file layout, tool surface, constraints, termination rules. ✓
- pipeline_starter.py: `ast.parse` OK. 4 stage stubs with docstrings and TODOs. main() runs all 4 stages. ✓
- trusted_packages.txt: 14 packages, one per line, each with `# reason`. ✓
- logs/agent_actions.log: 2 timestamped init lines. ✓
- dashboard/index.html: 0 external CSS/JS. 5 required sections present. Theme toggle with localStorage persistence. Inline `<style>` and `<script>`. ✓
- asset_research: 6 test-excerpt + 13 eval-set sources with primary-source-verified licenses. DCMLab license conflict + MAESTRO/MAPS NC clauses flagged. Recommended picks: MusicNet (30s test), Slakh2100+URMP+CocoChorales (eval). ✓

---

## Hot Take from each report

- **arXiv:** MuScriptor (arXiv:2607.08168) is the first open-weight multi-instrument AMT foundation model that actually works on real-world music, trained with the LLM-era 3-stage recipe (synthetic pretrain → real fine-tune → RL). Honorable mentions: VocalParse (LALM singing transcription, ships code), MuseAgent-1 (architecture pattern), MuSViT (sheet-music ViT).
- **GitHub:** omnizart/omnizart is **gone (404)** — first 404 on the watchlist. langgraph's switch from mypy to Astral `ty` is the most consequential non-feature change. basic-pitch is effectively abandoned. crewAI's setuptools bump addresses PYSEC-2026-3447.
- **Asset:** MusicNet is the right 30-second test source (CC-BY 4.0, real perf audio, per-note labels). Slakh2100 + URMP + CocoChorales is the right 50-100 piece eval combo (CC-BY, with NC-flag on MAESTRO/MAPS for commercial use).

---

## Gaps & Drift to Surface

1. **Naming convention drift between AGENTS.md and the cron task spec.** The cron task body says `reports/weekly_YYYY-MM-DD.md` and `reports/monthly_YYYY-MM_pipeline_report.md`; the foundation agent's AGENTS.md (independently drafted) says `out/arxiv/arxiv-YYYY-MM-DD.md` and `out/github/github-YYYY-MM.md`. The actual files follow the **cron task spec** (`reports/weekly_2026-08-01_arxiv.md`). I should reconcile AGENTS.md to the cron task spec before the next cycle, OR keep the cron task spec as the source of truth and have AGENTS.md defer to it. **Decision: keep cron task spec as canonical; edit AGENTS.md to match.**
2. **No assets/ populated yet.** The pipeline_starter.py references `assets/sample.wav` which doesn't exist. Cycle 2 should download a real 30-second MusicNet clip into `assets/`.
3. **No real pipeline execution yet.** The pipeline_starter.py is a stub. Cycle 2 or 3 should attempt the actual Demucs + Basic-Pitch chain on the sample clip (likely in a Docker/sandbox env, not on the macOS host — Demucs pulls torch).
4. **MuScriptor (arXiv:2607.08168) is the obvious Cycle-2 integration target.** It's open-weight, ships code, and is the first open multi-instrument AMT that beats YourMT3+ on real-world audio. The agent task spec asks for "Try replacing Basic Pitch with Omnizart for drums" — but Omnizart is dead; MuScriptor is the more relevant replacement for the *multi-instrument* stage. Worth proposing this swap to the human operator.
5. **Dagstuhl ChoirSet + URMP license is CC-BY (attribution required).** If any rendered output is shown publicly (e.g., a demo with attribution stripped), we need to preserve attribution in the dashboard.
6. **Aria-MIDI license is unclear** ("research use"). Flag for orchestrator decision before adopting.

---

## Next actions (per spec: 1–2 concrete suggestions for human operator)

1. **Replace `basic-pitch` with `MuScriptor`** in the pipeline's audio→MIDI stage. Justification: Basic Pitch is dormant (last release 2023-04-10) and is single-note / monophonic-leaning; MuScriptor is the first open-weight multi-instrument AMT that works on real-world music and ships with `pip install muscriptor`. New entry in `trusted_packages.txt`: `muscriptor # Open multi-instrument AMT foundation model (Rouard et al. 2026, arXiv:2607.08168)`. Requires: human approval for trusted_packages.txt change.
2. **Drop `omnizart/omnizart` from the watchlist** (404) and **add `muscriptor/muscriptor`** to the GitHub pulse watchlist. The next weekly cycle will then track MuScriptor's release cadence.
3. **Adopt MusicNet as the canonical 30-second test source** (Cycle 2 deliverable: download one track, slice to 30s, store in `assets/test_audio/sample_30s.wav`). License: CC-BY 4.0, attribution to Zenodo record 5120004 in the dashboard footer.
4. **Reconcile AGENTS.md naming convention** with the cron task spec (`reports/weekly_YYYY-MM-DD.md` / `reports/monthly_YYYY-MM_pipeline_report.md` / `reports/quarterly_YYYY-QX_eval.md`) before the next cycle.

---

## Budget consumed

- Workers: 5 (within 4–8 sweet spot)
- Wall-clock: ~10 min (within 15 min weekly budget)
- Files written: 24 (5 reports + 5 STATUS.json + AGENTS.md + pipeline_starter.py + trusted_packages.txt + logs/agent_actions.log + dashboard/index.html + 10 .gitkeep placeholders)
- External tool calls: 0 (per worker — they used web_search/web_fetch as needed but those are not billed as external tool calls here)

---

## Termination

**pass-all fired. All 5 slices PASS. Cycle 1 is complete.**

The next routine to fire (per cron spec) is the **monthly pipeline rebuild** scheduled for the first Monday of August (2026-08-03 in 6 days, or 2026-08-04 in 7 days — clarify with operator). Cycle 2 should:
- Drop omnizart from the GitHub watchlist
- Add muscriptor/muscriptor to the GitHub watchlist
- Download one MusicNet 30s clip into assets/test_audio/
- (Optionally) attempt a real Demucs + Basic-Pitch dry-run on the sample (sandboxed)
- Produce `reports/monthly_2026-08_pipeline_report.md`

State of termination rule: **pass-all** is active and was met. Cycle closes.
